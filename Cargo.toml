[package]
authors = ["Fraser Hutchison <fraser.hutchison@maidsafe.net>"]
description = "A hasher which is designed to work with already-hashed data."
documentation = "http://fraser999.github.io/Hash-Hasher"
license = "MIT/Apache-2.0"
name = "hash_hasher"
readme = "README.md"
repository = "https://github.com/Fraser999/Hash-Hasher"
version = "0.1.0"

[dependencies]
clippy = {version = "0.0", optional = true}

[dev-dependencies]
rand = "0.3"
